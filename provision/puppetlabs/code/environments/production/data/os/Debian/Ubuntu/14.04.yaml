---

classes:
    - ssh

ssh::storeconfigs_enabled: true
ssh::server_options:
        PasswordAuthentication: 'no'
        PubkeyAuthentication: 'yes'
        AuthorizedKeysFile: '%h/.ssh/authorized_keys'

profile::tomcat::server::version: '7.0.52-1ubuntu0.10'
